'use strict';

module.exports = function () {
  let init = function () {
    // let isNeed = document.querySelector('[data-module-required="counters"]');
    //
    // if (!isNeed) {
    //   return;
    // }
  };

  return {
    init: init,
  };
}();
