{# 🚨<b>{{ project.name }}</b>: <code>{{ event.message }}</code>

{% if times > 1 %}

    <b>{{ times }} times</b>
{% endif %} #}
{{ event.message|escape }}

<code>{{ event.errorLocation.file|escape }} -> {{ event.errorLocation.line|escape }}:{{ event.errorLocation.file|col }}</code>
{% if times > 1 %}

<b>{{ times }} times</b>
{% endif %}

{% set url = serverUrl ~ '/garage/' ~ userData.projectUri ~ '/event/' ~ event.groupHash %}
<a href="{{ url|escape }}">View Traceback</a> | <b>{{ project.name|escape }}</b> | 🚨 Hawk
