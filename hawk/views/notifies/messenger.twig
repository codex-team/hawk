🙀 <b>We have catched {{ times > 1 ? times ~ ' errors' : 'an error' }} on {{ domain }}</b>

<code>{{ event.message }}</code>

{{ event.errorLocation.full }}

{% set url = serverUrl ~ '/garage/' ~ domain ~ '/event/' ~ event.groupHash %}
<a href='{{ url }}'>{{ url }}</a>
