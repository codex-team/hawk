{% extends 'layout.twig' %}

{% block body %}
  <div class="yard">
    {% include 'yard/form.twig' with {
      action: '/join',
      title: 'Create account',
      fields: [
        {
          label: 'Enter your email',
          name: 'email',
          value: '',
          placeholder: 'name@best-team.com',
          type: 'email'
        }
      ],
      submitText: 'Register',
      desclaimer: 'Already have an account? <a href="/login">Login</a>, you, beauty.',
      message: message.type and message.text ? {type: message.type, text: message.text} : null,
    } %}
  </div>
{% endblock %}
